{
    "name": "printappclient",
    "version": "1.0.0",
    "description": "",
    "main": "src/client.js",
    "scripts": {
        "deploy": "node scripts/deploy.mjs",
        "deploy:client": "node scripts/deploy.mjs client",
        "deploy:all": "node scripts/deploy.mjs all",
        "deploy:run-cache": "node scripts/deploy.mjs run-cache",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "autoprefixer": "^10.4.12",
        "tailwindcss": "^3.1.8"
    }
}
